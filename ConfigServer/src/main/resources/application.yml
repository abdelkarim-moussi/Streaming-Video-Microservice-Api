spring:
  application:
    name: "ConfigServer"
  cloud:
    config:
      server:
        git:
          uri: https://github.com/abdelkarim-moussi/video-streaming-micro-config
          default-label: main

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8888
